import React from 'react';

import PageHeader from './PageHeader';

function PageReservoir () {
    let source = {label:'Reservoir'};

    return (
        <div>
          <PageHeader source={source}/>
        </div>
    );
}

export default PageReservoir;
