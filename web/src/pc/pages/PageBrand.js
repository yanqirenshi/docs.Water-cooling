import React from 'react';

import PageHeader from './PageHeader';

function PageBrand () {
    let source = {label:'Brand'};

    return (
        <div>
          <PageHeader source={source}/>
        </div>
    );
}

export default PageBrand;
