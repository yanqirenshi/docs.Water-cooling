import React from 'react';

import PageHeader from './PageHeader';

function PageShop () {
    let source = {label:'Shop'};

    return (
        <div>
          <PageHeader source={source}/>
        </div>
    );
}

export default PageShop;
