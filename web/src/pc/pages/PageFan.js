import React from 'react';

import PageHeader from './PageHeader';

function PageFan () {
    let source = {label:'Fan'};

    return (
        <div>
          <PageHeader source={source}/>
        </div>
    );
}

export default PageFan;
