import React from 'react';

import PageHeader from './PageHeader';

function PageFittings () {
    let source = {label:'Fittings'};

    return (
        <div>
          <PageHeader source={source}/>
        </div>
    );
}

export default PageFittings;
