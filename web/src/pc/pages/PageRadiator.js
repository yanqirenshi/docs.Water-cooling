import React from 'react';

import PageHeader from './PageHeader';

function PageRadiator () {
    let source = {label:'Radiator'};

    return (
        <div>
          <PageHeader source={source}/>
        </div>
    );
}

export default PageRadiator;
