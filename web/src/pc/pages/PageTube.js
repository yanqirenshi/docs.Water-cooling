import React from 'react';

import PageHeader from './PageHeader';

function PageTube () {
    let source = {label:'Tube'};

    return (
        <div>
          <PageHeader source={source}/>
        </div>
    );
}

export default PageTube;
